# 고시원 수익률 자동계산기 — 인수인계 문서

작성일: 2026-08-28

## 프로젝트 개요

`고시원임장보고서 (본인판단) 최종.xlsx` (사용자 보유, 17개 매물 시트 + `양식` 템플릿)에 들어있던 고시원 매물 수익률 계산 로직을, 매번 엑셀 복사 없이 브라우저에서 입력·계산·저장할 수 있는 단일 HTML 웹앱으로 재구현.

- **결과물**: `index.html` (이 폴더, 단일 파일, 프레임워크/빌드 없음)
- **배포**: Claude Artifact로 공개 게시 → https://claude.ai/code/artifact/645b0710-9dfb-4d12-9095-6adde84df992
  - claude.ai에서 호스팅되므로 다른 사람이 다른 IP/기기에서도 접속 가능, 모바일에서도 정상 동작 확인함.
  - 각 방문자는 자기 브라우저의 localStorage에 자기 데이터만 저장 (공유/동기화 아님).
  - 별도 과금 여부는 확인할 도구가 없어 claude.ai 청구 설정에서 직접 확인 권장.
  - 완전한 설치형 PWA는 Artifact 샌드박스 특성상 불가 (GitHub Pages로 옮겨야 함) — 사용자가 "지금 상태로 충분, 홈화면 추가만" 선택, 추가 작업 안 함.

## 확정된 범위/스펙 (v2, 사용자 승인 완료)

1차 버전(엑셀 전체 섹션 그대로 이식)이 "너무 복잡하다"는 피드백을 받아 v2로 전면 축소:

- **화면에 남긴 핵심 입력**: 방유형(미니룸/샤워룸/원룸)별 방개수·방별임대료, 보증금, 권리금, 임차료(월세), 관리비, 입실률(1개 슬라이더/숫자)
- **상세설정(접힘, 기본값이면 안 건드려도 됨)**: 시설투자, 집기류, 중개수수료율(기본 0.9%), 방유형별 부대비용 단가(기본 미니50/샤워55/원룸60)
- **제외**: 기본사항(주소 등), Min/Mid/Max 협상용 GAP표, 임차료 ×1.1 마크업, 시세/시설확인/평가/사진 섹션
- **유지된 사용자 결정**:
  - 방유형(미니룸/샤워룸/원룸) 구분은 하나로 합치지 않고 그대로 유지
  - 연 수익률 분모에 **보증금 포함** (원본 엑셀 방식 그대로, 참고했던 Gemini 예시는 보증금 제외 방식이었으나 채택 안 함)
- **UI 스타일**: Gemini가 만들어준 토스(Toss) 스타일 모바일 카드 + 하단 고정(sticky) 결과창 레이아웃을 참고해 채택
- 매물 여러 개 저장/불러오기 유지 (`localStorage` key: `goshiwon_properties_v2`)

계산 로직 (엑셀과 대조 검증 완료, 변경 없음):
```
거래금액 = 보증금 + 권리금 + 임차료×100
적정중개수수료 = 거래금액 × 중개수수료율
초기투자금 합계 = 보증금 + 권리금 + 시설투자 + 집기류 + 적정중개수수료
매출(입실률 r) = Σ 방유형별(방개수 × 방별임대료 × r)
부대비용 = Σ 방유형별(방개수 × 단가)
월임대수익 = 매출 − (임차료+관리비) − 부대비용
연 수익률(%) = 월임대수익×12 / 초기투자금합계
권리금 회수기간(개월) = 권리금 / 월임대수익
```

**검증 데이터셋** (가람원룸텔, 고양주엽): 임차료 4070천원(할인전 3700), 관리비 1000, 원룸 27개(방별임대료 390), 보증금 30000, 권리금 145000, 중개수수료율 0.009 → 거래금액 582,000 / 적정중개수수료 5,238 / 초기투자금합계 180,238 / 90% 입실률 시 월임대수익 331.35만원 · 회수기간 ≈44개월 · 연수익률 22.1%. 향후 회귀 확인 시 이 숫자로 대조.

## Impeccable 디자인 크리틱 (완료)

`/impeccable critique` 실행 결과 (스냅샷: `.impeccable/critique/2026-08-28T02-01-17Z__index-html.md`, Design Health Score 19/40): P0 1건 + P1 3건 + P2 1건 발견, **전부 수정 완료**하고 Artifact 재게시함.

- **P0**: 순수익 텍스트 색상 조건(`profit<0`)과 회수기간 배지 조건(`profit>0`)이 서로 달라, 기본값(0원) 상태에서 파란 "0"과 빨간 "적자 예상"이 동시에 뜨는 모순 → 두 조건을 `profit>0`으로 통일
- **P1** 키보드 접근 불가: "상세설정"/"다른 입실률로 보기" 토글을 `<div onclick>`→진짜 `<button>`으로 교체, `aria-expanded` 연동
- **P1** 라벨 연결 누락: 17개 입력 필드에 `label for` 또는 `aria-label` 연결
- **P1** 저장 버튼 명암비 2.7:1 (WCAG AA 미달) → 전용 토큰 `--btn-primary-bg:#1d4ed8`/`--btn-primary-text:#fff`로 분리해 6.7:1로 개선
- **P2** 적자 상태일 때 안내 문구("입실률을 높이거나 권리금·임차료를 낮추면 회수 가능합니다") 추가

모두 브라우저 테스트(키보드 포커스, 명암비 실측, 가람원룸텔 데이터 재검증)로 확인 후 같은 Artifact URL에 재배포 완료. **크리틱에서 나온 이슈는 이걸로 전부 해소된 상태.**

## 부가 작업 — Gemini 수익화 아이디어 팩트체크

계산기와 연계한 수익화 아이디어 10선(Gemini 작성, 2차 개정본까지)을 팩트체크해서 Gemini에게 전달할 메시지를 작성함 (`스크래치패드/gemini_feedback.md`, 세션 종료 시 스크래치패드는 사라지므로 필요하면 재작성 필요 — 핵심 내용은 아래 요약).

핵심 결론:
- 가짜 수식(장식적 비유) 쓰지 말고 논리로 설명할 것
- 가장 큰 문제: 지금 계산기는 비공개 개인 링크라 실사용자가 사실상 0명 — 트래픽 확보 방안이 전제 없이 빠진 아이디어가 다수
- 현재는 서버/DB/결제 없는 순수 정적 페이지라 연락처 DB 수집, 유료 결제, 데이터 API 판매, IoT 어드민 SaaS는 지금 구조로 불가능 (별도 프로젝트 수준)
- 법적 검토 누락 지적: 개인정보보호법(제3자 제공 동의), 대출모집인 등록 규정, 공인중개사법, 통신판매업 신고
- 그나마 현실성 있는 건 유료 PDF 리포트, 구독 커뮤니티 정도 — 이것도 트래픽 확보가 먼저

**미결 상태**: 이 팩트체크를 Gemini에게 실제로 전달할지, 아니면 이어서 카페 소개 글 초안으로 넘어갈지 사용자에게 물었고 아직 답변 없음. 수익화 아이디어 자체를 실행할지 여부도 미결.

## 다음에 할 일 (우선순위 없음, 필요 시 사용자 지시 대기)

1. Gemini 팩트체크 메시지를 실제로 전달할지 / 카페 소개 글로 넘어갈지 사용자 확인
2. (선택) 수익화 아이디어 중 실행할 것이 있다면 트래픽 확보 방안부터 설계
3. 그 외 계산기 자체에 대한 추가 요청 없으면 이 프로젝트는 현재 안정 상태 (Artifact 배포 완료, 크리틱 이슈 전부 해소)
